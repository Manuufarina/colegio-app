import React from 'react';
import { Container, Typography } from '@mui/material';

const EventoDetail = () => {
  return (
    <Container>
      <Typography variant="h5">Detalle de Evento</Typography>
    </Container>
  );
};

export default EventoDetail;
