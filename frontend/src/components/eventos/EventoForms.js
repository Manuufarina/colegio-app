import React from 'react';
import { Container, Typography } from '@mui/material';

const EventoForms = () => {
  return (
    <Container>
      <Typography variant="h5">Formulario de Evento</Typography>
    </Container>
  );
};

export default EventoForms;
