import React from 'react';
import { Container, Typography } from '@mui/material';

const MensajeForm = () => {
  return (
    <Container>
      <Typography variant="h5">Formulario de Mensaje</Typography>
    </Container>
  );
};

export default MensajeForm;
