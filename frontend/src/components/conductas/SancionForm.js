import React from 'react';
import { Container, Typography } from '@mui/material';

const SancionForm = () => {
  return (
    <Container>
      <Typography variant="h5">Formulario de Sancion</Typography>
    </Container>
  );
};

export default SancionForm;
